# SpaceTimePy VSCode Extension 

This extension contain the tools to epxlore trace generated by SpaceTimePy and a extension of the Python debugger to use Omniscient Live Debugging on Python function

## Build

```cmd
npm i
npm run build
vsce package
```